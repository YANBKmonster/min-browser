# MinBrowser

极简 Chromium 浏览器，基于 Electron 构建。

## 使用

```bash
npm start
```

## 构建

```bash
npm run build
```

输出在 `dist/` 目录：
- `MinBrowser-*-Setup.exe` — 安装包
- `MinBrowser-*-portable.zip` — 便携版
- `win-unpacked/MinBrowser.exe` — 解压即用
